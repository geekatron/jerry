# WI-01-001: Rename manifest.json to plugin.json

| Metadata | Value |
|----------|-------|
| **ID** | wi-01-001 |
| **Phase** | 1 - Plugin Infrastructure |
| **Status** | PENDING |
| **Priority** | HIGH |
| **Created** | 2026-01-12 |

---

## Summary

Rename `.claude-plugin/manifest.json` to `.claude-plugin/plugin.json` to align with Claude Code standard plugin discovery mechanism.

---

## Tasks

- [ ] Verify current manifest.json exists and is valid JSON
- [ ] Rename `.claude-plugin/manifest.json` to `.claude-plugin/plugin.json`
- [ ] Validate JSON syntax post-rename
- [ ] Test plugin discovery with `claude-code --debug`

---

## Acceptance Criteria

- [ ] File `.claude-plugin/plugin.json` exists
- [ ] File `.claude-plugin/manifest.json` does NOT exist
- [ ] Plugin JSON is valid and parseable
- [ ] Claude Code recognizes plugin during discovery

---

## Verification Evidence

### Validation Commands
```bash
# Plugin file exists with correct name
test -f .claude-plugin/plugin.json && echo "OK"

# Old file removed
test ! -f .claude-plugin/manifest.json && echo "OK"

# JSON is valid
python3 -c "import json; json.load(open('.claude-plugin/plugin.json'))"
```

### Output Artifact
- **File**: `.claude-plugin/plugin.json`

---

## Relationships

| Type | ID | Description |
|------|----|-------------|
| depends_on | wi-00-007 | ADR defines this change |
| blocks | wi-01-002 | Schema update requires renamed file |
| related_to | GAP-002 | Addresses plugin manifest gap |

---

## Notes

This is the first step in Phase 1. Must be completed before schema updates.
