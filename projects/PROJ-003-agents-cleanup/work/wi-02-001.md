# WI-02-001: Add Frontmatter to worktracker SKILL.md

| Metadata | Value |
|----------|-------|
| **ID** | wi-02-001 |
| **Phase** | 2 - Skill Frontmatter |
| **Status** | DONE |
| **Completed** | 2026-01-12 |
| **Priority** | HIGH |
| **Created** | 2026-01-12 |

---

## Summary

Add YAML frontmatter to `skills/worktracker/SKILL.md` with name, description (including trigger phrases), version, and allowed-tools per Claude Code schema.

---

## Tasks

- [x] Read current worktracker SKILL.md content
- [x] Create YAML frontmatter block with required fields
- [x] Include specific trigger phrases in description
- [x] Define allowed-tools list
- [x] Prepend frontmatter to existing content
- [x] Validate YAML syntax
- [x] Test skill auto-invocation with trigger phrase

---

## Acceptance Criteria

- [x] SKILL.md starts with `---` YAML delimiter
- [x] Frontmatter includes `name: worktracker`
- [x] Frontmatter includes `description` with trigger phrases in quotes
- [x] Frontmatter includes `version: 1.0.0`
- [x] Frontmatter includes `allowed-tools` list
- [x] YAML is syntactically valid
- [x] Skill auto-invokes when user says "create work item"

---

## Verification Evidence

### Expected Frontmatter
```yaml
---
name: worktracker
description: This skill should be used when the user asks to "create work item",
  "track task", "list tasks", "update work status", or mentions work/task management.
version: 1.0.0
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
---
```

### Validation Commands
```bash
# File starts with frontmatter
head -1 skills/worktracker/SKILL.md | grep -q "^---" && echo "OK"

# YAML is valid
python3 -c "import yaml; yaml.safe_load(open('skills/worktracker/SKILL.md').read().split('---')[1])"
```

### Output Artifact
- **File**: `skills/worktracker/SKILL.md` (modified)

### Source Reference
- proj-003-e-001: SKILL.md schema requirements
- GAP-006: worktracker SKILL.md missing frontmatter

### Execution Evidence (2026-01-12)

**Frontmatter Added:**
```yaml
---
name: worktracker
description: |
  This skill should be used when the user asks to "create work item",
  "track task", "list tasks", "update work status", or mentions work/task management.
version: 1.0.0
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
---
```

**Validation Output:**
```
$ head -1 skills/worktracker/SKILL.md | grep -q "^---" && echo "OK"
OK

$ grep -q "^name:" skills/worktracker/SKILL.md && echo "name: OK"
name: OK

$ grep -q "^description:" skills/worktracker/SKILL.md && echo "description: OK"
description: OK

$ grep -q "^version:" skills/worktracker/SKILL.md && echo "version: OK"
version: OK

$ grep -q "^allowed-tools:" skills/worktracker/SKILL.md && echo "allowed-tools: OK"
allowed-tools: OK
```

**Trigger Phrases Included:**
- "create work item"
- "track task"
- "list tasks"
- "update work status"
- "work/task management"

---

## Relationships

| Type | ID | Description |
|------|----|-------------|
| depends_on | wi-01-004 | Phase 1 must be complete |
| related_to | GAP-006 | Addresses high-severity gap |
| parallel | wi-02-002 | Can be done in parallel |
| parallel | wi-02-003 | Can be done in parallel |
| parallel | wi-02-004 | Can be done in parallel |

---

## Notes

Trigger phrases must be in quotes for Claude to recognize them for auto-invocation.
