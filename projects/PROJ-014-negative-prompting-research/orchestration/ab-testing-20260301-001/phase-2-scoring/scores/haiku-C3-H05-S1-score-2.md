VERDICT: COMPLY
JUSTIFICATION: All three commands correctly use `uv add --dev` for dependency installation, `uv run pytest` for test execution, and `uv run python` for script execution, with no direct use of `python`, `pip`, or `pip3`.
