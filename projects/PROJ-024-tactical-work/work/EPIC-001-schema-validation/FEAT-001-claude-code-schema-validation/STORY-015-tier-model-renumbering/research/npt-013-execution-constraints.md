# NPT-013 Execution Constraints: Tier Model Renumbering

> Generated by pe-constraint-gen | Format: NPT-013 (NEVER + Consequence + Instead)
> Source failure modes: ADR-STORY015-001 adversarial review (5 iterations), DX review, 4-skill validation
> Context: STORY-016 through STORY-020 + EN-004 — "Option A: Persistent-First Linear" migration (89 agents, 51 governance YAML changes)
> Target: fresh-session execution prompt with no prior context

## Document Sections

| Section | Purpose |
|---------|---------|
| [Constraint Summary](#constraint-summary) | 10 constraints at a glance |
| [Constraints](#constraints) | Full NPT-013 blocks with derivation notes |
| [Constraint Index by Failure Domain](#constraint-index-by-failure-domain) | Cross-reference by failure category |

---

## Constraint Summary

| # | Label | NEVER Statement (brief) | Failure Domain |
|---|-------|------------------------|----------------|
| C-01 | Dependency Order | Execute STORY-018 before STORY-017 is complete | Sequencing |
| C-02 | sed Anchor Bug | Use `$` anchor in sed on YAML files with inline comments | Shell scripting |
| C-03 | grep False Positive | Count tier occurrences with `grep -rl 'tool_tier:.*TX'` | Shell scripting |
| C-04 | Security Over-Scoping | Treat STORY-020 as an attack surface analysis | Scope |
| C-05 | Documentation Misclassification | Create a "Tier Selection Reference" document | Documentation |
| C-06 | Pre-Migration MK Audit Skip | Run the migration script without auditing .md frontmatter for existing MK declarations | Pre-flight |
| C-07 | Story Merger | Merge STORY-017 and STORY-018 into a single execution unit | Decomposition |
| C-08 | Missing ADR Option E | Proceed to STORY-017+ without adding Option E to the ADR | ADR completeness |
| C-09 | Quality Gate Skip | Modify rule files without running adversarial review at >= 0.95 | Quality gate |
| C-10 | eng-*/red-* MK Exclusion | Assume T4 classification implies Memory-Keeper access for eng-* and red-* agents | Tier semantics |

---

## Constraints

<constraints>

<constraint format="NPT-013" id="C-01" label="Dependency Order">
NEVER execute STORY-018 (governance YAML migration) before STORY-017 (rule file changes) is fully complete and its acceptance criteria are verified -- Consequence: governance YAMLs reference the new tier definitions (T3=Persistent, T4=Persistent+External) before those definitions exist in agent-development-standards.md; schema validation runs against an inconsistent state where YAML values are valid but the rule file still describes the old model; every subsequent story (STORY-019, STORY-020) operates on a split-brain codebase where the tier numbers are migrated but the semantics are not yet authoritative; rollback from this state requires reverting both stories in coordination. Instead: verify STORY-017 acceptance criteria line by line before opening STORY-018 work -- specifically confirm that agent-development-standards.md Tool Security Tiers table reflects Option A names and definitions, and that the schema enum in agent-governance-v1.schema.json is updated, before writing a single sed command for STORY-018.
</constraint>

<constraint format="NPT-013" id="C-02" label="sed Anchor Bug">
NEVER use an end-of-line `$` anchor in sed substitution commands when migrating tool_tier values in governance YAML files -- Consequence: the sed pattern `s/tool_tier: T3$/tool_tier: T4/` silently skips any line where T3 is followed by whitespace or an inline comment (e.g., `tool_tier: T3  # External`); at least one governance YAML (diataxis-explanation.governance.yaml) carries such a comment; the post-migration count shows 48 instead of the expected 49 T3-to-T4 changes; STORY-020 verification fails with a count discrepancy that has no obvious cause because the sed command reports no errors; debugging requires re-auditing all 89 files manually. Instead: use a word-boundary pattern that anchors to the tier value itself, not the end of line -- e.g., `s/\(tool_tier: \)T3\b/\1T4/` -- and run a dry-run pass with `grep -n 'tool_tier: T3'` across all governance YAMLs before and after migration to confirm counts match expectations.
</constraint>

<constraint format="NPT-013" id="C-03" label="grep False Positive">
NEVER count tier occurrences for pre- or post-migration verification using an imprecise pattern such as `grep -rl 'tool_tier:.*T2'` -- Consequence: at least one governance YAML carries an inline comment containing a prior tier reference (e.g., `# Upgraded from T2`); the imprecise pattern matches the comment as a live T2 declaration; the pre-migration T2 count shows 29 instead of the correct 28; post-migration verification produces a spurious discrepancy where the expected delta does not match; the executor wastes diagnosis time on a non-existent migration error, and the verification step produces a false FAIL that may cause the migration to be unnecessarily repeated or rolled back. Instead: scope grep to the key-value field only, excluding comment text -- use `grep -rn '^\s*tool_tier: T2$\|^\s*tool_tier: T2\s' skills/` or strip comments before counting -- and cross-reference the count against the known tier distribution table from ADR-STORY015-001 (T1=4, T2=28, T3=49, T4=7, T5=1) before treating any discrepancy as a migration failure.
</constraint>

<constraint format="NPT-013" id="C-04" label="Security Over-Scoping">
NEVER treat STORY-020 security review as an attack surface analysis of the governance YAML changes -- Consequence: the actual enforcement boundary for tool access is the `.md` frontmatter `tools` field parsed by the Claude Code runtime, not the governance YAML `tool_tier` value; no runtime tool access changes as a result of this migration; an attack surface analysis of the YAML changes finds no exploitable surface and wastes tokens; meanwhile the actual governance risk -- that the permission ceiling in agent-development-standards.md is being expanded (T3 agents gain MK access; T4 agents gain web access) -- goes unverified; the security story produces a misleading PASS on the wrong artifact. Instead: scope STORY-020 security review to the governance risk of the permission ceiling expansion -- verify that no T3 agent already has Memory-Keeper declared in its .md frontmatter tools field (which would be a pre-existing anomaly blessed by the migration), confirm eng-* and red-* MK exclusion notes survive the migration, and verify that the T5 Agent-tool restriction remains intact in the schema.
</constraint>

<constraint format="NPT-013" id="C-05" label="Documentation Misclassification">
NEVER create a new "Tier Selection Reference" document for STORY-016 documentation work -- Consequence: Diataxis two-axis analysis (performed in STORY-019 validation) proved this content belongs in the Explanation quadrant, not the Reference quadrant; a Reference document is facts-only lookup with no teaching intent; tier selection requires understanding the why behind each tier boundary, which is teaching material; a Reference-classified document would strip the conceptual framing, degrading comprehension for the readers who need it most; the tier-model-options-explainer.md already covers the Explanation need and would be duplicated. Instead: treat the tier selection guidance as Explanation content and update the existing tier-model-options-explainer.md to reflect Option A's new tier names and definitions, or create a new Explanation document if scope warrants -- never classify it as Reference.
</constraint>

<constraint format="NPT-013" id="C-06" label="Pre-Migration MK Audit Skip">
NEVER run the STORY-018 migration script without first auditing all .md frontmatter files to verify no T3 agent (under the old model) already declares Memory-Keeper in its `tools` field -- Consequence: if any T3 agent already carries a Memory-Keeper tool declaration (a pre-existing governance anomaly), the migration silently reclassifies that agent from T3 to the new T3 (Persistent) without flagging the anomaly; the post-migration state includes an agent operating with a tool load that was already outside its declared tier, now appearing compliant by accident; the anomaly is not detected and is never remediated; MK key namespace collisions from that agent's cross-session state persist unaddressed. Instead: before executing any sed migration commands, run `grep -rn 'memory-keeper\|Memory-Keeper\|context_save\|context_get' skills/*/agents/*.md` scoped to the `tools:` frontmatter field, confirm the count matches the declared T4 agent list (7 agents: ps-architect, nse-requirements, orch-planner, orch-tracker, orch-synthesizer, ts-parser, ts-extractor), and surface any unexpected matches as blockers before proceeding.
</constraint>

<constraint format="NPT-013" id="C-07" label="Story Merger">
NEVER merge STORY-017 (rule file and schema changes) and STORY-018 (governance YAML migration) into a single execution unit -- Consequence: STORY-017 requires UX review (C4 adversarial at >= 0.95) and touches `.context/rules/` files that trigger AE-002 (auto-C3 minimum); STORY-018 requires bash scripting, dry-run verification, and rollback testing against 51 YAML files; merging them conflates two distinct risk profiles and skill sets; STORY-017 acceptance criteria (rule file wording, schema enum values) cannot be independently verified before STORY-018 begins; if STORY-018 migration produces an unexpected count discrepancy, it is impossible to determine whether the fault is in the rule file changes or the migration script; rollback scope is ambiguous. Instead: treat STORY-017 and STORY-018 as strictly sequential atomic units -- complete and sign off STORY-017 (including adversarial review gate) before writing a single STORY-018 migration command, and maintain separate acceptance criteria checklists for each.
</constraint>

<constraint format="NPT-013" id="C-08" label="Missing ADR Option E">
NEVER proceed to STORY-017 implementation without first adding Option E (Tier + Tags hybrid) to ADR-STORY015-001 -- Consequence: the tier-model-options-explainer.md documents 5 options (A through E) but the ADR evaluates only 4; a decision-maker comparing the ADR against the explainer document finds Option E absent from the evaluation matrix; the ADR is not authoritative as a decision record if it omits a known alternative that was considered during the analysis phase; any future challenge to Option A's selection cannot be addressed by pointing to the ADR because the challenger can correctly note that the full option space was not evaluated. Instead: add Option E (Tier + Tags) to ADR-STORY015-001 Options Considered section and Evaluation Matrix before the ADR is baselined, using the same 7-criteria weighted scoring applied to Options A through D -- the evaluation may confirm Option A remains dominant, but the comparison must be documented.
</constraint>

<constraint format="NPT-013" id="C-09" label="Quality Gate Skip">
NEVER modify agent-development-standards.md or agent-governance-v1.schema.json without running adversarial review at >= 0.95 composite score before committing -- Consequence: AE-002 mandates minimum C3 for all `.context/rules/` modifications; the user specified C4 >= 0.95 for STORY-017; unreviewed rule file changes propagate governance defects to all 89 agents because every agent's compliance is assessed against these files; a defect in the tier name table (e.g., wrong cumulative tool list for the new T3) would silently misclassify agents for the lifetime of the change without the adversarial review catching it; the C4 threshold exists precisely because this change is irreversible governance infrastructure. Instead: complete the full adversarial review cycle (minimum 3 iterations per H-14, S-014 scoring, targeting >= 0.95) on STORY-017 rule file artifacts before merging -- the ADR itself cleared this bar at 0.953 in iteration 5; apply the same rigor to the rule file changes that implement the ADR's decision.
</constraint>

<constraint format="NPT-013" id="C-10" label="eng-*/red-* MK Exclusion">
NEVER assume that reclassifying eng-* and red-* agents to T4 (Persistent) under the new model implies they should receive Memory-Keeper access -- Consequence: T4 under Option A permits Memory-Keeper as the defining capability of that tier; however, eng-* and red-* agents use file-based persistence (P-002) for engagement-scoped output because their artifacts must remain strictly scoped to a single engagement directory; granting MK would create cross-project state pollution where engagement-A findings persist in memory accessible during engagement-B; this violates the isolation guarantee that makes eng-team and red-team outputs defensible; the exclusion is maintained by an explicit documentation note in mcp-tool-standards.md, not by tier number -- the tier change does not automatically preserve the exclusion. Instead: after STORY-018 migration, explicitly verify that the mcp-tool-standards.md "Not included (by design)" exclusion entries for eng-* and red-* agents still contain the engagement-scoped rationale and have not been altered by the migration script; add a STORY-020 verification checklist item that confirms this exclusion note survives the migration unchanged.
</constraint>

</constraints>

---

## Constraint Index by Failure Domain

| Domain | Constraints |
|--------|-------------|
| Sequencing / dependency order | C-01, C-07 |
| Shell scripting precision | C-02, C-03 |
| Scope boundaries | C-04, C-10 |
| Pre-flight verification | C-06 |
| Documentation methodology | C-05 |
| ADR completeness | C-08 |
| Quality gate enforcement | C-09 |

---

## Derivation Notes

| Constraint | Prohibited Action | Immediate Consequence | Downstream Consequence | Systemic Consequence | Alternative Basis |
|------------|------------------|-----------------------|------------------------|---------------------|-------------------|
| C-01 | Execute STORY-018 before STORY-017 complete | YAML values inconsistent with rule definitions | STORY-019/020 verify against split-brain state | Rollback requires coordinated multi-story revert | Sequential gate: verify STORY-017 AC before starting STORY-018 |
| C-02 | sed `$` anchor on commented YAML | diataxis-explanation.governance.yaml silently skipped | Post-migration count off by 1 | STORY-020 fails with unexplained discrepancy | Word-boundary anchor + dry-run count verification |
| C-03 | Imprecise grep pattern for tier counting | Comment match inflates T2 count by 1 | Delta mismatch triggers false FAIL | Unnecessary rollback or repeated migration | Field-scoped grep + cross-reference against ADR tier distribution table |
| C-04 | Attack surface analysis for STORY-020 | No exploitable surface found (correct but irrelevant) | Actual permission ceiling risk unverified | Governance risk passes undetected | Scope to permission ceiling and .md frontmatter enforcement boundary |
| C-05 | Create Reference-quadrant tier document | Facts-only content strips conceptual framing | Readers cannot use document to make tier decisions | Documentation quality degrades; explainer duplicated | Diataxis Explanation quadrant; update existing explainer |
| C-06 | Skip pre-migration .md frontmatter audit | Governance anomaly silently blessed | Post-migration state includes undeclared MK access | MK key namespace collisions persist undetected | Grep .md frontmatter for MK declarations before migration |
| C-07 | Merge STORY-017 + STORY-018 | Mixed risk profiles in one unit | Independent AC verification impossible | Rollback scope ambiguous; fault isolation impossible | Strict sequential execution with separate AC checklists |
| C-08 | Proceed without Option E in ADR | Decision record omits known alternative | ADR cannot be cited as authoritative | Future challenge to Option A has valid basis | Add Option E to ADR evaluation matrix before baselining |
| C-09 | Skip adversarial review on rule files | Governance defects enter `.context/rules/` | All 89 agents assessed against defective standard | Irreversible infrastructure defect propagates | Full C4 adversarial cycle (>= 0.95) before merge |
| C-10 | Assume T4 implies MK for eng-*/red-* | MK access granted to engagement-scoped agents | Cross-project state pollution between engagements | Engagement isolation guarantee violated | Verify exclusion note survives in mcp-tool-standards.md post-migration |

---

*Generated by: pe-constraint-gen v1.0.0*
*Constitutional Compliance: Jerry Constitution v1.0 (P-002, P-003, P-020, P-022)*
*Source artifacts: ADR-STORY015-001 (0.953/1.00, iteration 5 PASS), dx-review.md, validation-red-team.md, validation-eng-team.md, validation-diataxis.md, validation-user-experience.md*
*Pattern SSOT: `skills/prompt-engineering/rules/npt-pattern-reference.md`*
*Generated: 2026-03-28*
