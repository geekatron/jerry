TITLE: nuclear-sop: who may invoke agents mid-procedure? (delegation redesign, PR #269) — internal ref PROJ-032/BUG-001

Assignees: victorlau1, malcolm-x-evo

**What this is about:** the nuclear-sop skill's execution agent (`sop-executor`) is instructed, at certain built-in pause points, to invoke *other* agents — e.g. calling a quality reviewer when a quality-gate pause fires. But in this framework a worker agent may not invoke further agents (delegation is one level deep: main session → worker, never worker → worker), and the same file says so verbatim: "It cannot spawn subagents, delegate to sop-verifier, or invoke any other agent." Two related defects compound this. The flagship example workflow (`skills/nuclear-sop/examples/c3-adr-workflow-definition.md`) requires outside agents mid-procedure with no way to suspend and resume the executor's step-tracking around them. Separately, the composed pattern the how-to guide (`skills/nuclear-sop/docs/howto-guides.md`) recommends reaches roughly 7 agent hops, exceeding the framework's three-hop routing ceiling.

**The design question to answer:** who invokes quality gates and external agents mid-procedure, and how does `sop-executor` suspend and resume its place-keeping around them? (Its state file has exactly one waiting-on-an-outside-agent status today — `IV-PENDING`, for the independent-verification pause — so any design that keeps mid-procedure composition adds new ones.) Candidate designs (see linked register): (a) the quality-gate pause returns control to the main session, which runs the reviewer and re-invokes the executor (the adjacent independent-verification pause in the same file already returns control to the main session this way and can serve as the model); (b) the main session executes any step that is an agent invocation, with the executor checking the returned result; or (c) drop mid-procedure agent composition entirely.

**Acceptable descope:** option (c), with the example workflow rewritten to match — the review found this to be a legitimate answer, provided the shipped text matches the reduced scope.

**Required under any chosen design, including the descope:** name `adv-scorer` (the actual S-014 quality-scoring agent) instead of `ps-critic` everywhere this pattern appears; publish a hop-count budget for the composed pattern; and state the dependency on the `/adversary` skill's interface explicitly.

---
**Tracking.**
**Severity:** critical.
**Status:** not maintainer-fixable — needs a design decision.
**Links** (both pinned to a commit on branch `feat/proj-032-nuclear-sop-review` of this repo; pushed, not part of PR #269's branch): Worktracker (this repo's internal work-item record): [BUG-001](https://github.com/geekatron/jerry/blob/b2cf29664a30c62266565fcd357a75fd0aaa675a/projects/PROJ-032-nuclear-sop-review/work/BUG-001-qg-hold-delegation-topology/BUG-001-qg-hold-delegation-topology.md). Full analysis with candidate designs: [remediation register, section REM-01](https://github.com/geekatron/jerry/blob/b2cf29664a30c62266565fcd357a75fd0aaa675a/projects/PROJ-032-nuclear-sop-review/work/EPIC-001-pr269-review/FEAT-002-remediation-verdict/STORY-004-remediation/remediation-register.md#rem-01-qg-hold-and-mid-procedure-delegation-topology).
**Blocks:** merge of PR #269, as one of 7 co-equal design blockers (#350–#356) that must all close, alongside an owner ruling on the routing-hop rule (H-36) and an independent re-review scoring ≥ 0.92 — full merge-condition list in the review's [verdict document](https://github.com/geekatron/jerry/blob/b2cf29664a30c62266565fcd357a75fd0aaa675a/projects/PROJ-032-nuclear-sop-review/work/EPIC-001-pr269-review/FEAT-002-remediation-verdict/STORY-005-verdict/pr269-verdict.md#conditions-for-merge-after-rework), pinned to the same branch commit as the links above (also posted on PR #269).

<!--
R4 note on the round-3 required edits:
- Edit 1 (IV-HOLD analog hint): APPLIED. Wording adapted to the issue's established plain-language terms — "main session" instead of "orchestrator" (matches the draft's "main session → worker" framing and sop-executor.md's own "Return to the main context orchestrator" step), and the hold-point codename spelled out as "independent-verification pause" per the posted-comment style rule (internal hold-point names spelled out or dropped for the PR audience).
- Edit 2 (named merge-condition citation): APPLIED. Commit-pinned blob URL to pr269-verdict.md#conditions-for-merge-after-rework at the same commit (b2cf2966...) as the other two links; verified the file exists at that commit via git cat-file.
- Edit 3 (PROCEDURE_STATE hint): APPLIED IN CORRECTED FORM — the report's literal wording is factually wrong against ground truth on two points. (1) "waiting-on-external-actor states, which do not exist today": PROCEDURE_STATE.template.yaml (PR worktree, line 47 status enum) already defines IV-PENDING, and register REM-01 G2 states "the state machine has only IV-PENDING as a waiting-on-external-actor status" — one such status exists; verbatim application would also contradict edit 1's claim that the IV pause already works this way. (2) "any answer will require": register REM-01 candidate (c) (drop composition, example rewritten) requires no new states; only designs keeping composition do (QG-PENDING / AGENT-PENDING analogs per the register preamble, scoped to candidate (a)/(b)). The applied parenthetical surfaces the identical gap truthfully: exactly one such status exists today, and any design keeping mid-procedure composition must add new ones.
-->
