TITLE: PROJ-032/BUG-011: nuclear-sop — lessons-learned files written as .yaml but documented as .md, silently breaking the feedback loop (fixed on your branch)

**What this is:** one of seven mechanical fixes (the other six: #357-#359, #361-#363) the maintainer applied to your PR #269 branch (`proj-0039-nuclear-engineer`) in commit `c07033ce`, tracked as BUG-011 (the maintainer's internal defect ID). Nothing to do unless you disagree with the fix.

**What was wrong:** the skill's "operating experience" (OE) loop (capture lessons after each run; retrieve them before the next) was inconsistent about its file format. The rules file and the write path use `docs/experience/{entry_id}.yaml`, but the post-job template, one behavioral baseline, and the worked example said `.md`. Entries written per those documents would be invisible to retrieval, silently zeroing the feedback loop the skill names as its key capability, and one worked-example acceptance criterion was unsatisfiable. The retrieval protocol was specified three ways, and the workflow definition's "Attachments" section was documented as runtime-written by the capture agent, which never did.

**What the fix changed:** `.yaml` is now the single file convention everywhere (template, baseline, example, both agents, mirror copies); the lessons-learned search protocol in `skills/nuclear-sop/agents/sop-brief.md` now matches the rules file; and `skills/nuclear-sop/agents/sop-capture.md` gains a step appending the lessons-learned reference to the workflow definition's Attachments section, as three documents promised.

**How to verify:** on `proj-0039-nuclear-engineer` (`git fetch origin proj-0039-nuclear-engineer` first if `c07033ce` is missing locally), run `git diff c07033ce^ c07033ce -- skills/nuclear-sop/{templates/POST_JOB_BRIEF.template.md,behavioral-baselines/bb-003-oe-feedback-loop-integrity.md,examples/c3-adr-workflow-definition.md,agents/sop-brief.md,agents/sop-capture.md}` (the commit bundles six unrelated fixes; an unscoped diff mixes them in — even this scoped diff's `sop-capture.md` hunk includes unrelated `execution_log_final` lines, tracked separately as #361), and confirm three things: (1) the template, baseline, and worked example now use only `docs/experience/{entry_id}.yaml`; (2) `agents/sop-capture.md` adds a step appending the OE entry reference to the workflow definition's Attachments section; and (3) `agents/sop-brief.md` Step 4 now searches by `workflow_id` first (Glob, then filter), applying `workflow_type` only as a post-read filter, never the initial search key. CI at that commit: 15/15 green (https://github.com/geekatron/jerry/actions/runs/31174766440).

---
**Tracking:** worktracker (the maintainer's internal issue tracker) `projects/PROJ-032-nuclear-sop-review/work/BUG-011-oe-artifact-contract` (register section REM-11 in `remediation-register.md` on branch `feat/proj-032-nuclear-sop-review`). That branch is the maintainer's internal record; everything you need to act is above. Fix is already on your branch; this issue stays open until PR #269's disposition is decided.
