TITLE: nuclear-sop: duplicate agent definitions drifted apart, weakening a security guard (fixed on your branch) [PROJ-032/BUG-013]

**What this is:** one of seven fixes the maintainer applied directly to your PR #269 branch (`proj-0039-nuclear-engineer`) in commit `c07033ce`. No action needed now (comment on this issue if you disagree with the fix). Deleting `composition/` outright instead of keeping it synced is a choice deferred to your later rework pass, not part of this fix. Siblings: #357–#361, #363 (map: Cluster Index in `remediation-register.md`, path below).

**What was wrong:** each agent ships in four files (two under `agents/`, two under `composition/`) with no precedence rule, and the copies drifted apart — worst on security behavior. The skill's prompt-injection guard (SEC-001) shipped at three strengths: stop-work in the agent file (undercut by a contradictory "proceed unchanged" tail this fix deleted), log-and-proceed in the composition prompt, and absent from the composition YAML's forbidden actions. The verifier's composition prompt (214 lines vs. the 324-line agent file) dropped the caller-responsibility notice (a note that context isolation depends on the orchestrator building a clean Task prompt, not on this agent alone), the context-isolation contract (rules for what a delegated sub-agent's prompt must never contain), and the runtime self-check (the agent halts itself before any Task-tool use, Write/Edit/Bash, or delegation attempt). Other copies lost a Bash read-only restriction, routing keywords, and deviation-classification rules. Meanwhile PLAYBOOK.md labeled the never-loaded `composition/` copy "(canonical format)."

**What the fix changed:** every `composition/` file now carries a **derived artifact** header — the normative source is `agents/{name}.md` plus `agents/{name}.governance.yaml`, which is what Claude Code loads (`plugin.json`'s agents array lists only the `.md` files; `.governance.yaml` is a companion governance file, not part of the plugin manifest) — and drifted content was resynchronized from the strongest source: injection guard restored to full stop-work in every copy, isolation-contract text restored verbatim, and forbidden-action sets made identical.

**How to verify:** after `git fetch origin proj-0039-nuclear-engineer` (the commit went straight to your branch), run `git diff c07033ce^ c07033ce -- skills/nuclear-sop/composition/ skills/nuclear-sop/agents/ skills/nuclear-sop/SKILL.md skills/nuclear-sop/PLAYBOOK.md`. The commit bundles all seven fixes (REM-08–REM-14), so unrelated hunks appear; this bug's changes are limited to `composition/`, `agents/sop-executor.md`, `agents/sop-brief.governance.yaml`, `SKILL.md`, and `PLAYBOOK.md`. CI at that commit: 15/15 green — https://github.com/geekatron/jerry/actions/runs/31174766440.

---
**Tracking:** on branch `feat/proj-032-nuclear-sop-review` — worktracker `projects/PROJ-032-nuclear-sop-review/work/BUG-013-composition-drift/BUG-013-composition-drift.md`; register section REM-13 in `remediation-register.md` under `projects/PROJ-032-nuclear-sop-review/work/EPIC-001-pr269-review/FEAT-002-remediation-verdict/STORY-004-remediation/`. The fix is already on your branch; this issue stays open until PR #269's disposition is decided.
