# Gap Closure Execution Prompt — PROJ-036 Test Harness

> **Generated by:** pe-builder (5-element prompt anatomy)
> **Project:** PROJ-036
> **Quality validated by:** pe-scorer (7-criterion rubric)
> **Status:** AWAITING USER REVIEW — do not execute until approved

---

## Agent Routing Constraint (NPT-013)

```xml

<forbidden_actions>
  <constraint format="NPT-013">
    NEVER use the generic "general-purpose" subagent_type when invoking the Agent tool
    for any creator, reviewer, or synthesis task in this prompt -- Consequence: generic
    agents lack Jerry Framework skill context, governance rules, constitutional compliance,
    and domain methodology; outputs will miss H-07 architecture compliance, H-20 test
    standards, security hardening patterns, and quality gate enforcement, requiring
    complete rework. Instead: use the exact jerry:* subagent_type from the Agent Type
    Mapping Table below for every Agent tool invocation.
  </constraint>
  <constraint format="NPT-013">
    NEVER omit the subagent_type parameter when calling the Agent tool -- Consequence:
    omitting subagent_type defaults to "general-purpose", which triggers the constraint
    above. Instead: always specify subagent_type explicitly using the mapping table.
  </constraint>
</forbidden_actions>
```

## Agent Type Mapping Table

Every Agent tool invocation in this prompt MUST use the `subagent_type` from this table. No exceptions.

| Prompt Role | Agent Tool `subagent_type` | Purpose |
|---|---|---|
| eng-backend (creator) | `jerry:eng-backend` | Backend implementation: entry points, API validation, input hardening, model resolution, fixtures, extractors, dependency governance |
| eng-security (creator) | `jerry:eng-security` | Security code review: typed exception hierarchy, pre-batch health checks, zero-array assertions |
| eng-infra (creator) | `jerry:eng-infra` | Infrastructure hardening: Docker SHA pinning in CI workflows |
| eng-qa (creator) | `jerry:eng-qa` | Test engineering: unit tests, integration tests, coverage enforcement |
| eng-reviewer (final gate) | `jerry:eng-reviewer` | Final review gate: architecture compliance, coverage, security, test pass rate |
| adv-scorer (reviewer) | `jerry:adv-scorer` | S-014 LLM-as-Judge quality scoring with 6-dimension rubric |
| red-vuln (final gate) | `jerry:red-vuln` | Vulnerability re-assessment: attack surface comparison against Phase 2B baseline |
| ps-synthesizer (synthesis) | `jerry:ps-synthesizer` | Cross-pollination synthesis: aggregate all scores, evidence, and deltas into final report |
| orch-planner (orchestration) | `jerry:orch-planner` | Orchestration planning: decompose work items, sequence by dependency |

### Example Agent Tool Invocation

```
Agent(
  description="CG-001 layer4_stats entry point",
  subagent_type="jerry:eng-backend",
  prompt="Add argparse main() + if __name__ == '__main__' to jerry/testing/layer4_stats.py...",
  run_in_background=true
)
```

---

## Prompt

```
CONTEXT:
  The gap analysis is COMPLETE. Do NOT re-analyze, re-research, or re-run the gap analysis.
  All findings, work items, and RFA items are already documented.

  Primary data source (read-only reference):
    projects/PROJ-036-prompt-regression-harness/orchestration/gap-analysis-20260307-001/GAP-ANALYSIS-REPORT.md

  Work items (FEAT-036-003 children):
    projects/PROJ-036-prompt-regression-harness/work/EPIC-036-001-test-harness/FEAT-036-003-gap-closure/FEAT-036-003-gap-closure.md

  RFA source (Phase 3 quality score, 9 improvement recommendations):
    projects/PROJ-036-prompt-regression-harness/orchestration/gap-analysis-20260307-001/adversary/phase-3/adv-scorer-001/quality-score.md

  Completed: CG-003 (field name mismatch — already fixed in .github/workflows/).
  Deferred: CG-004 / BUG-036-002 (API key stays local until user has a key manager).

  Final synthesis output (NEW — produced by this execution):
    projects/PROJ-036-prompt-regression-harness/orchestration/gap-closure-20260307-001/GAP-CLOSURE-SYNTHESIS-REPORT.md

INSTRUCTIONS:
  Use /worktracker to update FEAT-036-003 status to "in_progress".

  Use /orchestration with orch-planner to decompose the 5 active work items below
  into /eng-team and /red-team agent tasks. Sequence by dependency.

  All creator agents MUST run in background. Main context is orchestrator only.
  Testing scoped to ps-researcher + ps-analyst only (2 of 5 agents).

  AGENT ROUTING (MANDATORY):
    Every Agent tool call MUST specify the subagent_type parameter from the
    Agent Type Mapping Table above this prompt block. NEVER use "general-purpose"
    or omit subagent_type. The correct types are jerry:eng-backend, jerry:eng-security,
    jerry:eng-infra, jerry:eng-qa, jerry:eng-reviewer, jerry:adv-scorer, jerry:red-vuln,
    jerry:ps-synthesizer, and jerry:orch-planner.

  ADVERSARIAL VALIDATION PATTERN (applies to every creator agent):
    AFTER each creator agent completes, launch /adversary adv-scorer (background) to review
    that creator's output. The adversary runs AFTER the creator, not alongside it.
    The flow per creator is: creator writes code → adversary reviews output + runs code execution → score.

    Each adv-scorer review:
      1. Reads the creator's completed output files
      2. Performs code review of the changes (S-014 rubric, 6 dimensions)
      3. Runs code execution validation where applicable (uv run pytest on new/modified test files,
         uv run python -c "import ..." for new modules, uv run python -m for __main__ blocks)
      4. Threshold: >= 0.92 per creator agent
      5. If score < 0.92: creator revises based on adv-scorer findings, then adversary re-reviews
         (creator-critic loop, max 3 iterations per H-14)

    Within a work item, all creators run in parallel. After ALL creators finish,
    all adversary reviews launch in parallel (one per creator's output).

  CROSS-SYNTHESIS PATTERN (after all work items pass):
    Use /prompt-engineering with pe-builder to construct the synthesis prompt for the final report.
    Use ps-synthesizer to cross-pollinate all per-agent adversary scores, code execution results,
    and work item completion evidence into:
      GAP-CLOSURE-SYNTHESIS-REPORT.md
    This report is NEW and distinct from the gap-analysis GAP-ANALYSIS-REPORT.md.
    It documents what was DONE, not what was FOUND.

    The synthesis report must include:
      - Per-work-item completion status with adv-scorer scores
      - Code execution evidence (test pass/fail, import validation, __main__ smoke)
      - Before/after quality delta (0.737 baseline from Phase 3 → post-closure score)
      - Security posture delta (Phase 2B baseline → post-closure red-vuln reassessment)
      - Remaining items (STORY-036-005 docs, CG-004 deferred, CG-009 deferred)

  Orchestration plan output:
    projects/PROJ-036-prompt-regression-harness/orchestration/gap-closure-20260307-001/ORCHESTRATION_PLAN.md

---

WORK ITEM 1: BUG-036-001 — Missing __main__ entry points (P0 Blocker)
  CGs: CG-001, CG-002 (CG-003 already done)
  GAP-ANALYSIS-REPORT ref: L1 §4 Sprint 0, L0 Critical Blocker #1, L2 PAT-002

  Creator A — eng-backend (background):
    Add argparse main() + if __name__ == "__main__" to jerry/testing/layer4_stats.py.
    Flags: --agent, --tier, --results-file, --head-sha, --bonferroni-k, --output-report, --output-markdown.
    Match the invocation pattern in .github/workflows/prompt-regression-full.yml.

  Creator B — eng-backend (background):
    Add argparse main() + if __name__ == "__main__" to jerry/testing/baselines/store.py.
    Flags: --action (store|retrieve|audit|invalidate), --agent, --results-file, --commit-sha, --tier, --reason.
    Match the invocation pattern in .github/workflows/prompt-regression-full.yml.

  Review A — adv-scorer (background, after Creator A completes):
    Validate Creator A output. Code execution: uv run python -m jerry.testing.layer4_stats --help
    Score: S-014 >= 0.92. If < 0.92, Creator A revises (max 3 iterations).

  Review B — adv-scorer (background, after Creator B completes):
    Validate Creator B output. Code execution: uv run python -m jerry.testing.baselines.store --help
    Score: S-014 >= 0.92. If < 0.92, Creator B revises (max 3 iterations).

  Persist scores: orchestration/gap-closure-20260307-001/adversary/bug-036-001/
  Use /worktracker to update BUG-036-001 status to "complete" when both pass.

---

WORK ITEM 2: STORY-036-001 — Security hardening (P1 Critical)
  CGs: CG-005, CG-006, CG-007, CG-016, CG-017, CG-018, CG-025, CG-027
  RFA included: CG-005 (SEC-002 typed exceptions), CG-006 (SEC-003 API key validation)
  GAP-ANALYSIS-REPORT ref: L1 §2 Security Posture, §4 Sprint 1, L2 Pattern 2, PAT-001

  Creator A — eng-security (background):
    CG-005: Typed exception hierarchy.
    Replace three nested except Exception in jerry_geval_deepeval_metric.py and deepeval_adapter.py with:
      EvaluationConfigError (auth/config — must fail CI)
      EvaluationAPIError (transient — retry)
      EvaluationScoringError (scoring failure — 0.0 fallback OK)
    Create jerry/testing/evaluation/exceptions.py.
    Add pre-batch health check (PAT-001 from GAP-ANALYSIS-REPORT L2 §3).
    Add post-batch zero-array assertion (>20% zeros = fail).

  Creator B — eng-backend (background):
    CG-006, CG-016: API key validation + telemetry opt-out.
    Add ANTHROPIC_API_KEY env check to DeepEvalAdapter.__post_init__; raise EnvironmentError.
    Add DEEPEVAL_TELEMETRY_OPT_OUT=YES to .env.example and all 3 CI workflow env: blocks.

  Creator C — eng-backend (background):
    CG-017, CG-018, CG-025, CG-027: Input validation hardening.
    CG-017: 8000-char output truncation at LLMTestCase construction.
    CG-018: Newline sanitization in _emit_gha_outputs + agent_id regex ^[a-z][a-z0-9_-]*$.
    CG-025: Allowed-root validation in _persist_report.
    CG-027: version_key format validation ^[0-9a-f]{7,40}:[^\n\r\0]+$.

  Creator D — eng-infra (background):
    CG-007: Docker SHA pinning.
    Pin all Docker image references to SHA digests in 3 workflow files.

  Review A — adv-scorer (background, after Creator A completes):
    Validate Creator A. Code execution: uv run python -c "from jerry.testing.evaluation.exceptions import EvaluationConfigError, EvaluationAPIError, EvaluationScoringError"
    Score: S-014 >= 0.92.

  Review B — adv-scorer (background, after Creator B completes):
    Validate Creator B. Code execution: uv run python -c "from jerry.testing.evaluation.deepeval_adapter import DeepEvalAdapter"
    Score: S-014 >= 0.92.

  Review C — adv-scorer (background, after Creator C completes):
    Validate Creator C. Code execution: review input validation patterns, verify regex correctness.
    Score: S-014 >= 0.92.

  Review D — adv-scorer (background, after Creator D completes):
    Validate Creator D. Review SHA digest format in workflow files.
    Score: S-014 >= 0.92.

  Persist scores: orchestration/gap-closure-20260307-001/adversary/story-036-001/
  Use /worktracker to update STORY-036-001 status to "complete" when all 4 pass.

---

WORK ITEM 3: STORY-036-003 — Model resolution quality gate compliance (P2 High)
  CGs: CG-013, CG-014, CG-023, CG-024
  ALL 4 are RFA items from Phase 3 quality score (0.737 REVISE)
  GAP-ANALYSIS-REPORT ref: L1 §3 Quality Score, §4 Sprint 3

  Creator A — eng-backend (background):
    CG-013: Change self.model.startswith("claude") to self.model.lower().startswith("claude").
    CG-023: Add code comment in _resolve_model() referencing gap-analysis-20260307-001 CG-013.
    CG-024: Add Bedrock/Vertex detection — if model matches ^anthropic\.claude, raise ValueError.

  Creator B — eng-backend (background):
    CG-014: Create tests/prompt-regression/unit/test_resolve_model.py with 4 cases:
      "claude-sonnet-4-20250514" → AnthropicModel
      None → None
      "gpt-4" → "gpt-4"
      "Claude-Sonnet" (capitalized) → AnthropicModel

  Review A — adv-scorer (background, after Creator A completes):
    Validate Creator A. Code execution: uv run python -c "from jerry.testing.evaluation.jerry_geval_deepeval_metric import JerryGEvalDeepEvalMetric"
    Score: S-014 >= 0.92.

  Review B — adv-scorer (background, after Creator B completes):
    Validate Creator B. Code execution: uv run pytest tests/prompt-regression/unit/test_resolve_model.py -v
    Score: S-014 >= 0.92.

  Persist scores: orchestration/gap-closure-20260307-001/adversary/story-036-003/
  Use /worktracker to update STORY-036-003 status to "complete" when both pass.

---

WORK ITEM 4: STORY-036-002 — Integration pipeline (P2 High)
  CGs: CG-008, CG-010, CG-011, CG-012
  CG-009 (baseline population) deferred to after integration tests confirm pipeline works.
  Depends on: BUG-036-001 complete (entry points must exist).
  GAP-ANALYSIS-REPORT ref: L1 §4 Sprint 2, L2 Pattern 1

  Creator A — eng-backend (background):
    CG-011: Add @pytest.fixture to tests/prompt-regression/conftest.py returning DeepEvalAdapter as EvaluationPort.
    Configure model_name from ANTHROPIC_MODEL env var or claude-sonnet-4-20250514 default.

  Creator B — eng-backend (background):
    CG-008: Create jerry/testing/extraction/promptfoo_extractor.py + __init__.py.
    extract_score_arrays(promptfoo_json_path) -> dict[str, tuple[ScoreArray, ScoreArray]].

  Creator C — eng-backend (background):
    CG-010: Add build_metric_for_mr(mr: MetamorphicRelation) -> JerryGEvalDeepEvalMetric to DeepEvalAdapter.

  Creator D — eng-backend (background):
    CG-012: Create .github/actions/cost-monitor/action.yml and .github/actions/artifact-publish/action.yml stubs.

  Review A — adv-scorer (background, after Creator A completes):
    Validate Creator A. Code execution: uv run python -c "import tests.prompt_regression.conftest" (import check).
    Score: S-014 >= 0.92.

  Review B — adv-scorer (background, after Creator B completes):
    Validate Creator B. Code execution: uv run python -c "from jerry.testing.extraction.promptfoo_extractor import extract_score_arrays"
    Score: S-014 >= 0.92.

  Review C — adv-scorer (background, after Creator C completes):
    Validate Creator C. Code execution: uv run python -c "from jerry.testing.evaluation.deepeval_adapter import DeepEvalAdapter; hasattr(DeepEvalAdapter, 'build_metric_for_mr')"
    Score: S-014 >= 0.92.

  Review D — adv-scorer (background, after Creator D completes):
    Validate Creator D. Check action.yml files parse as valid YAML.
    Score: S-014 >= 0.92.

  Persist scores: orchestration/gap-closure-20260307-001/adversary/story-036-002/
  Use /worktracker to update STORY-036-002 status to "complete" when all 4 pass.

---

WORK ITEM 5: STORY-036-004 — Quality verification + dependency governance (P3 Medium)
  CGs: CG-019, CG-020, CG-021, CG-022, CG-026, CG-028, CG-029, CG-030
  Testing scoped to ps-researcher + ps-analyst only.
  Depends on: Work Items 1-4 all complete.
  GAP-ANALYSIS-REPORT ref: L1 §4 Sprint 4

  Creator A — eng-qa (background):
    Unit tests covering all Work Item 1-4 code changes:
      main() argparse (layer4_stats, baselines/store)
      _resolve_model() 4 cases
      DeepEvalAdapter.__post_init__ API key validation
      Exception hierarchy behavior
      Output truncation (8000 chars)
      version_key format validation
    Target: >= 90% line coverage on modified files (H-20).

  Creator B — eng-qa (background):
    Integration tests scoped to ps-researcher + ps-analyst:
      End-to-end pytest via conftest evaluator fixture
      Score array extraction from sample promptfoo output
      Layer4Pipeline smoke path with 2-agent data
      Baseline store round-trip (store + retrieve)

  Creator C — eng-backend (background):
    CG-028: Narrow deepeval bound to >=3.8.0,<4.0.0 in pyproject.toml.
    CG-029: Add scipy>=1.11.0 explicit dependency in pyproject.toml.
    CG-030: Add uv run pip-audit step to Smoke workflow.

  Review A — adv-scorer (background, after Creator A completes):
    Validate Creator A. Code execution: uv run pytest tests/prompt-regression/unit/ -v --tb=short
    Score: S-014 >= 0.92 AND all tests pass.

  Review B — adv-scorer (background, after Creator B completes):
    Validate Creator B. Code execution: uv run pytest tests/prompt-regression/integration/ -v --tb=short
    Score: S-014 >= 0.92 AND all tests pass.

  Review C — adv-scorer (background, after Creator C completes):
    Validate Creator C. Code execution: uv sync (verify dependency resolution succeeds).
    Score: S-014 >= 0.92.

  Persist scores: orchestration/gap-closure-20260307-001/adversary/story-036-004/
  Use /worktracker to update STORY-036-004 status to "complete" when all 3 pass.

---

FINAL GATE: Security Re-Assessment + Cross-Synthesis

  Step 1 — Security delta (parallel, background):

    Use /red-team with red-vuln (background):
      Re-assess 3 attack surfaces (API Key, Prompt Injection, Supply Chain) after all fixes.
      Compare against GAP-ANALYSIS-REPORT L1 §2 Security Posture baseline.
      Verify STORY-036-001 security fixes are effective.
      Output: orchestration/gap-closure-20260307-001/red/final/red-vuln-001/security-reassessment.md

    Use /eng-team with eng-reviewer (background):
      Final review gate:
        All work items complete (5 active, 1 deferred, 1 pre-complete)
        H-07 architecture compliance
        H-20 coverage >= 90%
        No new security findings
        Test pass rate 100%
      Output: orchestration/gap-closure-20260307-001/eng/final/eng-reviewer-001/final-review.md

  Step 2 — Cross-synthesis (after Step 1 completes):

    Use /prompt-engineering with pe-builder to construct the synthesis prompt, then
    use ps-synthesizer to cross-pollinate ALL adversary scores, code execution results,
    security reassessment, and eng-reviewer findings into the final report:

      GAP-CLOSURE-SYNTHESIS-REPORT.md
      at: projects/PROJ-036-prompt-regression-harness/orchestration/gap-closure-20260307-001/GAP-CLOSURE-SYNTHESIS-REPORT.md

    The synthesis report structure (L0/L1/L2):

      L0 Executive Summary:
        - Work items completed (count, pass rate)
        - Quality delta: 0.737 (gap-analysis baseline) → post-closure composite
        - Security delta: Phase 2B findings mitigated vs remaining
        - Remaining: STORY-036-005, CG-004, CG-009

      L1 Technical Details:
        - Per-work-item table: work item ID, creator agents, adv-scorer scores, code execution results
        - Per-CG resolution status: fixed | deferred | out-of-scope
        - Test coverage summary (H-20 compliance)
        - Dependency governance changes (pyproject.toml delta)

      L2 Strategic Assessment:
        - Before/after quality comparison (dimension-level: 0.737 breakdown → new breakdown)
        - Security posture delta (Phase 2B → post-closure)
        - Patterns resolved (which of the 4 L2 patterns from GAP-ANALYSIS-REPORT are closed)
        - Remaining risk register
        - Recommendations for STORY-036-005 (docs) and CG-009 (baseline population)

    Use /prompt-engineering with pe-scorer to score the synthesis prompt >= 90 on the 7-criterion rubric
    before executing it. Iterate with pe-builder if score < 90.

  Step 3 — Completion:
    Use /worktracker to update FEAT-036-003 status to "complete" after synthesis report is written.
    Update ORCHESTRATION_PLAN.md Phase Status table with all results.

---

STORY-036-005 (P4 Documentation — CG-015, CG-031–CG-034) is OUT OF SCOPE for this prompt.
BUG-036-002 / CG-004 (API key) is DEFERRED per user direction.
CG-009 (baseline population) is DEFERRED until pipeline is validated.
Handle these in a separate follow-up prompt.
```

---

## Prompt Anatomy (pe-builder 5-element validation)

| Element | Present | Value |
|---------|---------|-------|
| 1. Skill Routing | Yes | /worktracker, /orchestration + orch-planner, /eng-team (eng-backend, eng-security, eng-infra, eng-qa, eng-reviewer), /red-team (red-vuln), /adversary (adv-scorer), /prompt-engineering (pe-builder, pe-scorer) |
| 2. Scope | Yes | 5 active work items (BUG-036-001, STORY-036-001/002/003/004), RFA items embedded, 3 deferred |
| 3. Data Source | Yes | GAP-ANALYSIS-REPORT.md (read-only, no re-analysis) |
| 4. Quality Gate | Yes | adv-scorer S-014 >= 0.92 per creator (sequential review after completion), pe-scorer >= 90 on synthesis prompt, red-vuln + eng-reviewer final gate |
| 5. Output Path | Yes | GAP-CLOSURE-SYNTHESIS-REPORT.md (new), per-work-item adversary scores in orchestration/gap-closure-20260307-001/ |

## Enhancements Over v1

| Enhancement | How Addressed |
|------------|---------------|
| New synthesis report | GAP-CLOSURE-SYNTHESIS-REPORT.md — distinct from GAP-ANALYSIS-REPORT.md. Documents what was DONE, not what was FOUND. |
| /adversary per creator (sequential) | Each creator agent has a post-completion adv-scorer review. Pattern: Creator N completes → Review N validates output + runs code execution. |
| Code execution validation | Each adversary includes specific uv run commands: pytest for tests, python -c for imports, python -m for __main__ blocks. |
| Creator-critic loop | If adv-scorer < 0.92, creator revises (max 3 iterations per H-14). |
| Cross-synthesis via /prompt-engineering | pe-builder constructs synthesis prompt → pe-scorer validates >= 90 → ps-synthesizer executes → produces L0/L1/L2 report. |
| Quality delta tracking | Synthesis report compares 0.737 baseline (Phase 3) to post-closure composite, dimension by dimension. |
| Security delta tracking | Synthesis report compares Phase 2B baseline to post-closure red-vuln reassessment. |

## Dependency Order

```
BUG-036-001 (P0 entry points)
    ↓
STORY-036-001 (P1 security)  ←─ parallel ─→  STORY-036-003 (P2 model resolution RFA)
    ↓                                              ↓
STORY-036-002 (P2 integration pipeline)
    ↓
STORY-036-004 (P3 quality verification + deps)
    ↓
FINAL GATE: red-vuln + eng-reviewer → pe-builder + ps-synthesizer → GAP-CLOSURE-SYNTHESIS-REPORT.md
```

## Execution Notes

- **15 creator agents + 15 sequential adversary reviews** across all work items
- **This prompt is designed for a fresh session** to maximize context budget
- **Main context orchestrates only** — tracks work item status, launches agents, enforces phase gates
- **Each adversary persists its score** to orchestration/gap-closure-20260307-001/adversary/{work-item-id}/
- **Agent routing enforced** — NPT-013 constraint forbids generic agents; all Agent tool calls must use jerry:* subagent_type from the mapping table
