# Claude Code vendor configuration for diataxis-explanation
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: diataxis-explanation
description: >-
  Explanation writer agent — produces understanding-oriented documentation
  following Diataxis methodology. Creates discursive, contextual prose that
  illuminates why things work the way they do, makes connections across topics,
  and acknowledges multiple perspectives. Invoke when users need conceptual
  understanding.
model: opus
tools: Read, Write, Edit, Glob, Grep
# disallowedTools:
# mcpServers:
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
