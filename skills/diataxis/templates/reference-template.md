# {Subject} Reference

> {One-sentence description of what this reference covers.}

<!-- Quality criteria: skills/diataxis/rules/diataxis-standards.md Section 1 (R-01 through R-07) -->
<!-- Anti-patterns to avoid: RAP-01 (marketing claims), RAP-02 (instructions/recipes), RAP-03 (narrative explanation) -->
<!-- Voice: Neutral, precise, austere. No opinions, no superlatives. See Section 5. -->

## Overview

{One to three sentences maximum. State what system or component this reference covers. Neutral declarative sentences only. No opinions, design rationale, or historical context -- those belong in the companion Explanation document.}

## {Category 1 — name MUST mirror the described system's own terminology (R-01)}

<!-- Add as many entries as needed per category. Every public interface must be documented (R-07). -->
<!-- Each entry MUST follow the same structure (R-05). -->
<!-- R-01: Category names MUST mirror the described system's own structure -- for code: module/class names; for CLI: command groups; for config: section names. -->

### `{entry-name}`

**Type:** {type}
**Default:** {default value}
**Required:** {yes/no}
**Since:** {version or date introduced — optional, include for versioned systems}

{Neutral description of what this entry does.}

**Example:**

```
{illustrative usage showing the entry in context — NOT a procedural recipe}
```

### `{entry-name}`

**Type:** {type}
**Default:** {default value}
**Required:** {yes/no}
**Since:** {version or date introduced — optional, include for versioned systems}

{Neutral description of what this entry does.}

**Example:**

```
{illustrative usage showing the entry in context — NOT a procedural recipe}
```

## {Category 2 — name MUST mirror the described system's own structure (R-01)}

<!-- Repeat the entry format for each category. Add categories as needed. -->

### `{entry-name}`

**Type:** {type}
**Default:** {default value}
**Required:** {yes/no}
**Since:** {version or date introduced — optional, include for versioned systems}

{Neutral description of what this entry does.}

**Example:**

```
{illustrative usage showing the entry in context — NOT a procedural recipe}
```

<!-- R-07 Completeness Checklist: Before finalizing, verify every public interface/command/parameter/field is documented. -->

## Related

- **Tutorial:** [Learn to use {subject}]({path}) -- Guided introduction
- **How-To Guide:** [How to {common task}]({path}) -- Task-oriented instructions
- **Explanation:** [About {subject design}]({path}) -- Design rationale and context
