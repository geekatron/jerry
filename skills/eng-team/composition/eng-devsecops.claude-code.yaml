# Claude Code vendor configuration for eng-devsecops
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: eng-devsecops
description: >-
  DevSecOps pipeline engineer for the /eng-team skill. Invoked when users
  request automated security scanning (SAST/DAST), CI/CD security pipeline
  configuration, secrets scanning, container scanning,
model: sonnet
tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch
# disallowedTools:
mcpServers:
  context7: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
