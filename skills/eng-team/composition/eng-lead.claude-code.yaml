# Claude Code vendor configuration for eng-lead
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: eng-lead
description: >-
  Engineering lead and standards enforcer for the /eng-team skill. Invoked when
  users request implementation planning, code standards enforcement, dependency
  governance, or technical quality
model: sonnet
tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch
# disallowedTools:
mcpServers:
  context7: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
