# Claude Code vendor configuration for eng-security
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: eng-security
description: >-
  'Security code review specialist for the /eng-team skill. Invoked when users
  request manual secure code review, security requirements verification, or
  architecture security review. Produces
model: sonnet
tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch
# disallowedTools:
mcpServers:
  context7: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
