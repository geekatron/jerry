# Claude Code vendor configuration for nse-requirements
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: nse-requirements
description: >-
  NASA Requirements Engineer agent implementing NPR 7123.1D Processes 1, 2, and
  11 for stakeholder needs, requirements definition, and requirements
  management, with adversarial quality mode integration
model: sonnet
tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch
# disallowedTools:
mcpServers:
  memory-keeper: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
