# Post-Job Brief: {workflow_id}

> Generated by sop-capture. This document closes the execution record for workflow `{workflow_id}` and captures operating experience for future pre-job briefings.

## Document Sections

| Section | Purpose |
|---------|---------|
| [Execution Summary](#execution-summary) | Workflow identity, timestamps, step counts, stop-work events |
| [Deviation Log](#deviation-log) | All deviations with classification, resolution, and root cause |
| [Hold Point Record](#hold-point-record) | Hold point activations and SR-05 anomaly notation |
| [Verification Outcome](#verification-outcome) | IV disposition (C1-C2 integrated or C3+ sop-verifier) |
| [Operating Experience Entry](#operating-experience-entry) | OE entry reference and schema |
| [Lessons Learned](#lessons-learned) | Structured lessons derived from this execution |
| [Improvement Recommendations](#improvement-recommendations) | Workflow definition changes recommended for next execution |

---

## Execution Summary

| Field | Value |
|-------|-------|
| **Workflow ID** | `{workflow_id}` |
| **Workflow Type** | `{NOMINAL | ABNORMAL | EMERGENCY}` |
| **Criticality** | `{C1 | C2 | C3 | C4}` |
| **Verification Mode** | `{3-hop (integrated IV) | 4-hop (sop-verifier)}` |
| **Started At** | `{ISO-8601 UTC}` |
| **Completed At** | `{ISO-8601 UTC}` |
| **Elapsed** | `{HH:MM:SS}` |
| **Total Steps Planned** | `{N}` |
| **Steps Completed** | `{N}` |
| **Steps Deviated** | `{N}` |
| **Hold Points Activated** | `{N}` |
| **Stop-Work Events** | `{N}` |
| **Deviation Classification** | `{NONE | MINOR | MAJOR | STOP-WORK}` |
| **Final Status** | `{COMPLETED | ABORTED}` |
| **OE Entry ID** | `{entry_id}` |
| **PROCEDURE_STATE.yaml** | `{path}` |
| **Execution Log (FINAL)** | `{path}` |

---

## Deviation Log

<!-- sop-capture: populate from STAR Review STOP-WORK entries in execution log. If deviation_type is NONE, write "No deviations logged during this execution." and retain the table header for consistency. -->

| Step | Deviation Type | Description | Resolution | Root Cause |
|------|----------------|-------------|------------|-----------|
| {N} | `{MINOR | MAJOR | STOP-WORK}` | {description of what deviated from procedure} | {how it was resolved, or "Procedure aborted"} | {root cause of this specific deviation} |

> **Deviation Classification (overall):** `{NONE | MINOR | MAJOR | STOP-WORK}`
>
> Classification rules applied:
> - NONE: all steps completed per procedure; no STOP-WORK entries in execution log
> - MINOR: deviation corrected within procedure; all acceptance criteria met
> - MAJOR: stop-work required; user escalation occurred; some criteria may not be met
> - STOP-WORK: procedure abandoned before completion

---

## Hold Point Record

<!-- sop-capture: populate from PROCEDURE_STATE.yaml hold activation history and HOLD_POINT_LOG.md. Apply SR-05 cross-reference: for every hold point defined in workflow definition, verify activation record exists in both execution log AND PROCEDURE_STATE.yaml. Flag anomalies below. -->

**Reference:** Full hold point log at `{path/to/HOLD_POINT_LOG.md}`

### Activated Hold Points

| Step | Hold Type | Activated At | Resolution | Released At |
|------|-----------|-------------|------------|------------|
| {N} | `{USER-HOLD | QG-HOLD | IV-HOLD}` | `{ISO-8601}` | `{APPROVED | REJECTED | WAIVED | ACCEPTED}` | `{ISO-8601}` |

### SR-05 Hold Point Consistency Check

> **SR-05 Requirement:** Every hold point defined in the workflow definition must have a corresponding activation record in BOTH the execution log AND PROCEDURE_STATE.yaml.

| Hold Point (from workflow definition) | Execution Log Record | PROCEDURE_STATE.yaml Record | SR-05 Status |
|--------------------------------------|---------------------|----------------------------|-------------|
| Step {N}: `[{USER-HOLD | QG-HOLD | IV-HOLD}]` | `{FOUND at line N | MISSING}` | `{FOUND | MISSING}` | `{CONSISTENT | HOLD_POINT_NOT_ACTIVATED}` |

**Anomalies (HOLD_POINT_NOT_ACTIVATED):**

<!-- sop-capture: list any hold points where SR-05 status is HOLD_POINT_NOT_ACTIVATED. If none, write "No SR-05 anomalies detected. All workflow-defined hold points have corresponding activation records." -->

| Anomaly | Hold Point | Workflow Definition Reference | Escalation Required |
|---------|-----------|-------------------------------|---------------------|
| `HOLD_POINT_NOT_ACTIVATED` | Step {N}: `[{USER-HOLD | QG-HOLD | IV-HOLD}]` | {workflow definition section} | `{YES (USER-HOLD bypass) | NO (QG/IV -- log only)}` |

---

## Verification Outcome

<!-- sop-capture: choose the appropriate sub-section based on criticality. Delete the sub-section that does not apply. -->

### C1-C2 Integrated Independent Verification (3-Hop Mode)

> **ANCHORING BIAS DISCLAIMER:** This verification was performed by sop-capture, which has access to the execution log and STAR records. This differs from the context-isolated verification performed by sop-verifier in 4-hop mode (C3+). The verifier's conclusion may be influenced by the execution narrative. This limitation is accepted for C1-C2 work because execution outcomes are reversible within 1 session to 1 day.

**Overall IV Disposition:** `{ACCEPTED | REJECTED | ACCEPTED-WITH-CONDITIONS}`

| Acceptance Criterion | Source | Disposition | Notes |
|--------------------|--------|-------------|-------|
| {criterion text from pre-job brief} | `{work product path}` | `{MEETS | FAILS}` | {observation} |

**Criteria failed (if any):** {list criteria with FAILS disposition, or "None -- all criteria met"}

---

### C3+ Independent Verification (4-Hop Mode -- sop-verifier Report)

> sop-verifier performed context-isolated independent verification via Task tool. sop-capture reads the IV report produced by sop-verifier. sop-capture does not re-evaluate the work products.

**sop-verifier IV Report:** `{path from PROCEDURE_STATE.yaml iv_report_path}`

**IV Disposition (from sop-verifier report):** `{ACCEPTED | REJECTED | ACCEPTED-WITH-CONDITIONS}`

**IV Iteration:** `{1 | 2 | 3}` (max 3 before mandatory user escalation per P-020)

**Conditions (if ACCEPTED-WITH-CONDITIONS):** {list conditions from sop-verifier report, or "N/A"}

---

## Operating Experience Entry

**OE Entry ID:** `{entry_id}`

**Local capture path:** `capture/oe-entry-{entry_id}.md`

**Persistent path (future sop-brief retrieval):** `docs/experience/{entry_id}.md`

### OE Entry Schema

```yaml
oe_entry:
  # Identity
  entry_id: "{workflow_id}-{YYYYMMDD}-{NNN}"
  entry_version: "1.0.0"
  workflow_id: "{workflow_id}"
  workflow_type: "NOMINAL | ABNORMAL | EMERGENCY"
  criticality: "C1 | C2 | C3 | C4"
  created_at: "{ISO-8601 UTC}"

  # Execution summary
  total_steps: 0
  steps_completed: 0
  steps_deviated: 0
  hold_points_activated: 0
  stop_work_events: 0
  verification_mode: "3-hop | 4-hop"

  # Deviation classification
  deviation_type: "NONE | MINOR | MAJOR | STOP-WORK"

  # Knowledge content
  root_cause: "{root cause of most significant deviation, or 'N/A -- no deviation'}"
  recommendation: "{specific recommendation to improve workflow or process}"
  error_traps_encountered:
    - "{error trap ID or description from execution log, if any}"

  # Disposition
  verification_outcome: "ACCEPTED | REJECTED | ACCEPTED-WITH-CONDITIONS | N/A"
  quality_gate_final_score: null
```

> **Schema enforcement note:** All fields above are REQUIRED. sop-capture blocks the OE entry Write call if any field is missing or empty. The `error_traps_encountered` list may be empty (`[]`) but must be present. `quality_gate_final_score` may be `null` if no QG-HOLD was activated.

---

## Lessons Learned

<!-- sop-capture: derive lessons from root_cause, error_traps_encountered, and deviation_type. Each lesson must reference a specific observation from this execution. Do not write generic lessons (e.g., "be more careful") -- every lesson must be specific enough to inform the next sop-brief error trap identification. -->

| # | Lesson | Source Observation | Applicability |
|---|--------|--------------------|--------------|
| 1 | {specific lesson derived from this execution} | {execution log reference or deviation log row} | {this workflow only | all workflows of this type | all nuclear-sop workflows} |

**Pattern signals (if available):** {cross-reference against prior OE entries for this workflow_id, or "Insufficient prior OE entries for pattern analysis -- first execution."}

---

## Improvement Recommendations

<!-- sop-capture: derive recommendations from deviation_type, root_cause, and lessons learned. Recommendations must be specific workflow definition changes, not process aspirations. Each recommendation must identify the specific step, annotation, or section of the workflow definition that should be modified. -->

| Priority | Recommendation | Target | Rationale |
|----------|----------------|--------|-----------|
| `{HIGH | MEDIUM | LOW}` | {specific workflow definition change: add WARNING annotation at Step N, change step classification from REFERENCE to CONTINUOUS, add USER-HOLD at Step N, split sub-procedure at step N} | {workflow definition section or step number} | {traceability to specific deviation or lesson} |

**Workflow definition version recommendation:** `{no change | patch (typo/clarification) | minor (new annotation or hold point) | major (step resequencing or split)}`

**Recommended by:** sop-capture | `{entry_id}` | `{created_at}`
