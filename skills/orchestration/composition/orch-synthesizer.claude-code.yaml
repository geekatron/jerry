# Claude Code vendor configuration for orch-synthesizer
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: orch-synthesizer
description: >-
  Orchestration Synthesizer agent for cross-document synthesis, pattern
  extraction, and final workflow recommendations
model: sonnet
tools: Read, Write, Edit, Glob, Grep, Bash
# disallowedTools:
mcpServers:
  memory-keeper: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
