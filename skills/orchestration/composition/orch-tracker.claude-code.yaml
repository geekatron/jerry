# Claude Code vendor configuration for orch-tracker
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: orch-tracker
description: >-
  Orchestration State Tracker agent for updating workflow state, registering
  artifacts, and creating checkpoints
model: haiku
tools: Read, Write, Edit, Glob, Grep, Bash
# disallowedTools:
mcpServers:
  memory-keeper: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
