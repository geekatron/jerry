# Claude Code vendor configuration for ps-analyst
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: ps-analyst
description: >-
  Deep analysis agent for root cause, trade-offs, gap analysis, and risk
  assessment with adversarial quality strategies and L0/L1/L2 output levels
model: sonnet
tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch
# disallowedTools:
mcpServers:
  context7: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
