# Claude Code vendor configuration for ps-critic
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: ps-critic
description: >-
  Quality evaluation agent for creator-critic-revision cycles with adversarial
  strategy integration (S-014 LLM-as-Judge primary) - critiques agent outputs
  using SSOT quality dimensions and provides
model: sonnet
tools: Read, Write, Edit, Glob, Grep
# disallowedTools:
# mcpServers:
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
