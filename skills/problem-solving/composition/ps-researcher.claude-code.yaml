# Claude Code vendor configuration for ps-researcher
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: ps-researcher
description: >-
  Deep research agent with MANDATORY artifact persistence, PS integration,
  Context7 MCP, adversarial quality strategies, and L0/L1/L2 output levels
model: opus
tools: Read, Write, Edit, Glob, Grep, WebSearch, WebFetch, Bash
# disallowedTools:
mcpServers:
  context7: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
