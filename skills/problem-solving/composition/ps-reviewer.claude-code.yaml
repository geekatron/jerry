# Claude Code vendor configuration for ps-reviewer
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: ps-reviewer
description: >-
  Quality review agent for code, design, architecture, and security reviews with
  adversarial quality strategies and L0/L1/L2 output levels
model: sonnet
tools: Read, Write, Edit, Glob, Grep, Bash
# disallowedTools:
# mcpServers:
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
