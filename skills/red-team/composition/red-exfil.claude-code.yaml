# Claude Code vendor configuration for red-exfil
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: red-exfil
description: >-
  Data Exfiltration Specialist for /red-team. RoE-GATED -- requires explicit
  authorization in Rules of Engagement with specified data types. Provides
  methodology for data identification, exfiltration
model: sonnet
tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch
# disallowedTools:
mcpServers:
  context7: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
