# Claude Code vendor configuration for red-lateral
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: red-lateral
description: >-
  Lateral Movement Specialist for /red-team. Provides methodology for pivoting,
  tunneling, living-off-the-land techniques, and internal exploitation. Uses C2
  during operations but does NOT build
model: sonnet
tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch
# disallowedTools:
mcpServers:
  context7: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
