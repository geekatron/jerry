# Claude Code vendor configuration for red-recon
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: red-recon
description: >-
  Reconnaissance Specialist for /red-team. Performs OSINT, network enumeration,
  service discovery, technology fingerprinting, and attack surface mapping.
  Feeds adversary TTPs to eng-architect
model: sonnet
tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch
# disallowedTools:
mcpServers:
  context7: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
