# Claude Code vendor configuration for red-vuln
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: red-vuln
description: >-
  Vulnerability Analyst for /red-team. Performs vulnerability identification,
  CVE research, exploit availability assessment, attack path analysis, and risk
  scoring. Consumes reconnaissance findings
model: sonnet
tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch
# disallowedTools:
mcpServers:
  context7: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
