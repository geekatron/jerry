# Claude Code vendor configuration for sb-calibrator
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: sb-calibrator
description: >-
  Voice Fidelity Scorer — scores voice fidelity on a 0-1 scale across the 5
  voice traits, computes a composite voice fidelity score, and applies leniency
  bias counteraction consistent with adv-scorer
model: sonnet
tools: Read, Write, Edit, Glob, Grep
# disallowedTools:
# mcpServers:
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
