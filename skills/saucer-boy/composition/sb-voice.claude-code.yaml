# Claude Code vendor configuration for sb-voice
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: sb-voice
description: >-
  Session voice agent — generates McConkey-style conversational responses for
  work sessions. Produces ambient personality (acknowledgments, celebrations,
  explanations) and explicit persona responses
model: opus
tools: Read, Glob, Grep
# disallowedTools:
# mcpServers:
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
