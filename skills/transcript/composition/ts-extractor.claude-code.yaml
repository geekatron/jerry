# Claude Code vendor configuration for ts-extractor
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: ts-extractor
description: >-
  Extracts semantic entities (speakers, actions, decisions, questions, topics)
  from parsed transcripts
model: sonnet
tools: Read, Write, Glob
# disallowedTools:
mcpServers:
  memory-keeper: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
