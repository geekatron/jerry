# Claude Code vendor configuration for ts-mindmap-ascii
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: ts-mindmap-ascii
description: >-
  Generates ASCII art mindmap visualizations as fallback for non-Mermaid
  environments
model: sonnet
tools: Read, Write, Glob
# disallowedTools:
# mcpServers:
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
