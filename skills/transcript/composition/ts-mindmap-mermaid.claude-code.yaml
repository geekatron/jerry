# Claude Code vendor configuration for ts-mindmap-mermaid
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: ts-mindmap-mermaid
description: "Generates Mermaid mindmap visualizations from extracted transcript entities"
model: sonnet
tools: Read, Write, Glob
# disallowedTools:
# mcpServers:
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
