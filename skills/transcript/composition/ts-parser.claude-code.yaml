# Claude Code vendor configuration for ts-parser
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: ts-parser
description: >-
  Strategy Pattern orchestrator for hybrid parsing: Python delegation for VTT,
  LLM fallback for others
model: haiku
tools: Read, Write, Glob, Bash
# disallowedTools:
mcpServers:
  memory-keeper: true
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
