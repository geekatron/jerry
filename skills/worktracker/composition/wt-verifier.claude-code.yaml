# Claude Code vendor configuration for wt-verifier
# Layer 4 of 4: per-agent vendor-specific overrides.
# Reference: https://docs.anthropic.com/en/docs/claude-code/agent-tool

name: wt-verifier
description: "Verify work item acceptance criteria before closure"
model: sonnet
tools: Read, Glob, Grep, Write, Bash
# disallowedTools:
# mcpServers:
permissionMode: default
background: false
# maxTurns:
# skills:
# hooks:
# memory:
# isolation:
